
import styles from "styles/Home.module.scss"

export default function Sidebar() {
    return (
        <aside className={`${styles.sidebar} p-4 border bg-blue-100`}>
            Sidebar
        </aside>
    )
}
